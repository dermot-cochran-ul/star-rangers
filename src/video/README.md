# `src/video/` — video assets

Two files. One is referenced from a codex entry; `story-bible/video-prompts.md` records that the other is an orphan nothing references, and that neither has a prompt kept. The house motion signature and the camera-is-a-witness rule live in that note. Check what references a file (`grep -rn <name> src lib`) before adding, replacing or removing one.
