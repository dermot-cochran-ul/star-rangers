node_modules/
_site/
.DS_Store
*.log
deploy.conf
giscus.local.json
# An independent host's own edition registry - see sample-editions.local.json.
# Gitignored for the same reason as the two above: it lets someone run this site
# on their own domain without maintaining a fork, and without their config
# conflicting on every upstream merge.
editions.local.json
deploy-logs/
src/lore/custom/
# Firefly Services credentials. This repo is public and a client secret is a
# credential for the whole Adobe org - see scripts/firefly.local.json.sample.
scripts/firefly.local.json
# Raw Firefly generations: several MB per prompt, four variations each, and
# only one of them ever becomes a committed image (resized via import-image.ps1).
firefly-out/
