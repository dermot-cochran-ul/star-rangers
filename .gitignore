node_modules/
_site/
.DS_Store
*.log
deploy.conf
giscus.local.json
# An independent host's own edition registry - see sample-editions.local.json.
# Gitignored for the same reason as the two above: it lets someone run this site
# on their own domain without maintaining a fork, and without their config
# conflicting on every upstream merge.
editions.local.json
deploy-logs/
src/lore/custom/
# Image-prompt working directory: generated output, the run manifest, and the
# served-prompt order. Machine-local bookkeeping, never part of the repo.
image-out/
# Gemini API key for scripts/image-prompts.js --generate. Public repo: never
# commit this. See scripts/gemini.local.json.sample.
scripts/gemini.local.json
